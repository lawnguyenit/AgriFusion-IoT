# Model Suite Local Tests

Repository-facing automated tests live under `Backend/tests/`.

This directory is reserved for future lane-local fixtures or reference
artifacts when the suite grows beyond simple unit coverage.
