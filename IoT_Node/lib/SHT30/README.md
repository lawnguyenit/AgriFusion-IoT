# SHT30

Purpose: legacy low-level SHT30 helper library.

Main API:
- `setupSHT()`
- `readSHT()`
- `checkSHTConnection()`
- `controlHeater()`

Status:
- Kept mainly for older experiments and standalone testing.
- The current production flow uses `Sht30Service` instead.
